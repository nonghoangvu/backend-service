spring:
  config:
    activate:
      on-profile: dev
  devtools:
    add-properties: true
  datasource:
    url: jdbc:postgresql://${DATABASE_HOST:localhost}:${DATABASE_PORT:5431}/hoangvu
    username: ${DATABASE_USERNAME:hoangvu}
    password: ${DATABASE_PASSWORD:321}
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: none

# application-dev.yml
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
openapi:
  service:
    api-docs: backend-service
    server: http://localhost:${server.port}
    title: Backend service
    version: 1.0.0

# Generate HMAC Authentication Code: https://www.devglan.com/online-tools/hmac-sha256-online
jwt:
  expiryMinutes: 1
  expiryDay: 14
  accessKey: f2lbQFu3wDo3chQA71xo3UNt2TI83ntHsfI/GqnDKNM=
  refreshKey: 1K6mJcyMTWH7arbdTRAnQAx//1QiRnF3vae0MRptt0E=
